{
  "name": "tireflow",
  "version": "1.0.0",
  "description": "Sistema de controle de estoque e vendas de pneus via WhatsApp",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "test": "tsx --test tests/*.test.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test && npm run build && npx prisma validate",
    "check:runtime": "tsx src/runtimeCheck.ts",
    "backup": "tsx src/database/backup.ts",
    "restore": "tsx src/database/restoreBackup.ts",
    "service:install": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/install-nssm-service.ps1",
    "service:remove": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/remove-nssm-service.ps1",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio",
    "seed:products": "tsx src/database/seedProducts.ts"
  },
  "keywords": [
    "whatsapp",
    "bot",
    "tires",
    "inventory",
    "sales"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "dotenv": "^17.4.2",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.34.7"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "@types/qrcode-terminal": "^0.12.2",
    "prisma": "^6.19.3",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
